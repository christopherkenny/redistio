% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/draw.R
\name{draw}
\alias{draw}
\title{Interactive Plan Drawing}
\usage{
draw(
  shp,
  init_plan,
  ndists,
  palette,
  layers = NULL,
  pop_tol = 0.05,
  adj_col = "adj",
  split_cols = guess_admins,
  opts = redistio_options(),
  save_path = tempfile(fileext = ".csv")
)
}
\arguments{
\item{shp}{an \code{sf} tibble that you want to draw with}

\item{init_plan}{Plan to initialize with.}

\item{ndists}{Number of districts to draw if \code{init_plan} is not supplied.}

\item{palette}{Color palette to fill shapes with. Default is Polychrome 36.}

\item{layers}{Named list of \code{sf} objects to overlay. Also takes column names in \code{shp} to group by.}

\item{pop_tol}{the population tolerance.}

\item{adj_col}{Name of column in \code{shp} that contains adjacency information.}

\item{split_cols}{Name of column in \code{shp} that contain administrative units}

\item{opts}{list of options. Default is \code{redistio_options()}}

\item{save_path}{Output path to save progress to.}
}
\value{
Shiny app
}
\description{
Interactive Plan Drawing
}
\examples{
if (interactive()) {
  draw(dc, dc$ward)
  draw(dc, dc$ward, layers = list(neighborhoods = 'adv_nbr'))
}

}
